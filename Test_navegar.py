#esto hace que regrese a una pagina anterior, si el tiempo es muy largo, no funcionará
#driver.back(tiempo)

#esta funcion hace lo mismo que el driver.back(tiempo), solo que mejor
#driver.execute_script("window.history.go(-1)")

#driver.execute_script("window.history.go(2)")